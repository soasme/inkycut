# E2E Tests

The complete harness documentation lives in [docs/testing.md](../docs/testing.md).

Quick start:

```bash
npm run db:up
npm run db:migrate
npx playwright install chromium
npm run test:e2e
```
